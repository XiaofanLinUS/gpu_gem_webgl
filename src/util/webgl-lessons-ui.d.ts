namespace GL_UI {
    function updateUI(widgets: any, data: any): void;
    function setupUI(parent: any, object: any, uiInfos: any): any;
    function setupSlider(selector: any, options: any): any;
    function makeSlider(options: any): any;
    function makeCheckbox(options: any): any;
    function getQueryParams(): any;
}

//export {updateUI, setupUI, setupSlider, makeSlider, makeCheckbox, getQueryParams};
export default GL_UI;
